spring:
  application:
    name: cloud-service-zuul
  cloud:
    config:
      discovery:
        enabled: true
        service-id: cloud-config-server
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/